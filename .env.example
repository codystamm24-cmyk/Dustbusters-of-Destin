# Example environment variables for local development
DB_URL=jdbc:postgresql://localhost:5432/dustbusters
DB_USER=postgres
DB_PASS=mysecretpassword
# JWT_SECRET should be a base64-encoded random key (do NOT commit the real secret)
# Example to generate: openssl rand -base64 32
JWT_SECRET=
