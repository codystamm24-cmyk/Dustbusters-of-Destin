Mobile app (Expo / React Native)

Quick start (requires Node and Expo CLI):

1. cd mobile
2. npm install
3. npx expo start

Note: when running on emulator, the Java backend address may be `10.0.2.2` (Android emulator) or use your machine IP for physical device.
