Java backend (simple HTTP server)

Run locally:

1. Ensure you have Java 11+ installed.
2. From this folder run:

   chmod +x run.sh
   ./run.sh

The server listens on http://localhost:8080 and exposes GET /jobs
